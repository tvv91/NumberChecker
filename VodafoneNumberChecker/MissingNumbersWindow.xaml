<Window x:Class="VodafoneNumberChecker.MissingNumbersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Номера не найдены в sim_data"
        Height="520"
        Width="620"
        WindowStartupLocation="CenterOwner"
        MinHeight="420"
        MinWidth="520">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Импорт отменен. Следующие номера отсутствуют во внешней базе"
                   TextWrapping="Wrap"
                   FontSize="13"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1"
                   x:Name="countTextBlock"
                   FontSize="12"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8"/>

        <TextBox Grid.Row="2"
                 x:Name="numbersTextBox"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap"
                 FontFamily="Consolas"
                 FontSize="12"/>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="Копировать список"
                    Width="140"
                    Margin="0,0,8,0"
                    Click="CopyButton_Click"/>
            <Button Content="Закрыть"
                    Width="100"
                    IsDefault="True"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
